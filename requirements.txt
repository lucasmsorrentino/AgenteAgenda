# Core
python-dotenv>=1.0.0
pydantic>=2.12.0,<3.0.0
loguru>=0.7.3

# Telegram
python-telegram-bot>=22.6,<23.0

# Google Calendar
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# Anytype (REST API via httpx)
httpx>=0.28.0

# Scheduling (reminders inside bot process)
apscheduler>=3.10.0,<4.0.0

# --- Dev / test (optional, install with pip install -r requirements.txt) ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
